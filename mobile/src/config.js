// Set this to your backend URL.
//   - Same WiFi as the backend computer:    http://<LAN-IP>:8000
//   - Find your LAN IP on Linux:            hostname -I
//   - ngrok tunnel:                         https://<random>.ngrok-free.app
//
// Important: localhost / 127.0.0.1 will NOT work on a physical phone — those
// resolve to the phone itself, not your laptop.
export const API_URL = 'http://100.111.163.42:8000'
